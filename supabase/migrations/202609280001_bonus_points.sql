-- ==============================================================================
-- Migration: 202609280001_bonus_points.sql
--
-- النقط الإضافية (نظام النقط): نقط بيضيفها الطالب بنفسه — نقط بونص، أنشطة، أو
-- درجات خارجية مش جزء من درجات المواد.
--
-- القاعدة: النقط دي بتتضاف على النقط المُحصَّلة، ومش بتغيّر التوتال الكلي اللي
-- الطالب حدده في الإعدادات. يعني لو التوتال 435 نقطة وضاف 3 نقط، يفضل التوتال
-- 435 والنقط المُحصَّلة تزيد 3 (يقرب من التوتال).
--
-- الشكل: [{ id, points, note?, date, createdAt }]
-- وبتتحفظ كمان في صف الميتا (__student_spec_meta__) جوه grading_scale كنسخة
-- احتياطية، فالتطبيق شغال حتى قبل تنفيذ الـ migration دي.
-- ==============================================================================

ALTER TABLE public.settings
  ADD COLUMN IF NOT EXISTS bonus_points JSONB DEFAULT '[]'::jsonb;

NOTIFY pgrst, 'reload schema';
