import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test() { const payload = { id: 'test-minimal', user_id: 'test-user-id' }; const { error } = await supabase.from('notes').insert([payload]); if (error) console.error(error); else { const { data } = await supabase.from('notes').select('*').eq('id', 'test-minimal'); console.log(Object.keys(data[0]).join(', ')); await supabase.from('notes').delete().eq('id', 'test-minimal'); } } test();
