import dotenv from 'dotenv'; dotenv.config(); fetch(process.env.VITE_SUPABASE_URL + '/rest/v1/', { headers: { 'apikey': process.env.VITE_SUPABASE_ANON_KEY } }).then(r => r.json()).then(d => { console.log(Object.keys(d.definitions.schedule_items.properties).join(', ')); console.log(Object.keys(d.definitions.notes.properties).join(', ')); console.log(Object.keys(d.definitions.tasks.properties).join(', ')); console.log(Object.keys(d.definitions.appointments.properties).join(', ')); }).catch(e => console.error(e));
