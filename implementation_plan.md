# خطة تنفيذ التحديثات الشاملة (Admin & Warnings & Modals & Attachments)

## 1. حل مشكلة تسجيل دخول الأدمن
- في `src/App.tsx`:
  - إتاحة مسار `/admin` للوصول المستقل حتى إن لم يكن هناك جلسة طالب مسجلة في Supabase، أو في صفحة `Auth.tsx` التحقق المباشر من بيانات الأدمن (`admin@gmail.com` أو `admin@gmail.ocm` مع كلمة المرور `Body22@33`) وتسجيل دخوله كأدمن فوراً وتحويله إلى `/admin`.

## 2. تحويل شريط تنقل الأدمن إلى سايد بار (Admin Sidebar)
- في `src/pages/Admin.tsx`:
  - تحويل شريط التبويبات العلوي إلى سايد بار متكامل (لوحة تحكم جانبية) يضم (الإحصائيات، سجل الطلاب، صندوق المقترحات، والنسخ الاحتياطي والأتمتة).

## 3. إزالة الإيموجيز من فئات الشكاوى والمقترحات
- إزالة كافة الإيموجيز من اختيارات وعروض الفئات (اقتراح ميزة، شكوى، إبلاغ عن خطأ، عام) في `UserFeedbackSection.tsx` و `Admin.tsx`.

## 4. إرفاق المرفقات (Attachments) مع المقترحات والشكاوى
- تحديث `FeedbackSuggestion` في `src/types/index.ts` لدعم `attachments?: string[]` أو كائنات مرفقات.
- إضافة منتقي ملفات (File Picker) في `UserFeedbackSection.tsx` يتيح للطلاب إرفاق صور ومستندات.
- حفظ المرفقات في قاعدة البيانات وعرضها وتنزيلها من لوحة تحكم الأدمن في `Admin.tsx`.

## 5. تعديل إعدادات حد الإنذار إلى نافذة منبثقة ونظام عرض/تعديل/حفظ
- في `src/pages/academic/AcademicWarnings.tsx`:
  - إضافة زر "إعدادات حد الإنذار" يفتح Modal مخصص.
  - إتاحة العرض المقفل افتراضياً، مع زر "تعديل" لتغيير التقدير/النقاط وزر "حفظ".

## 6. استبدال رسائل المتصفح بنوافذ تأكيد وحذف مخصصة (Custom Modals)
- إنشاء مكون عام `ConfirmModal` و `PromptModal` وتطبيقه على الدرايف (حذف/تعديل الملفات والمجلدات)، والمقترحات، وكافة عمليات الحذف في المنصة دون استخدام `window.confirm` أو `window.prompt`.

## 7. كود Supabase Edge Function والأتمتة و SQL
- إنشاء كود Edge Function `send-database-backup` مع إرشادات تشغيل الـ Cron وتحديث كود SQL.
