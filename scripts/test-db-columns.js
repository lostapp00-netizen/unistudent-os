import { createClient } from '@supabase/supabase-js'; import { v4 as uuidv4 } from 'uuid'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test() { const userId = uuidv4(); const tables = ['schedule_items', 'appointments', 'notes', 'tasks']; for (const t of tables) { const payload = { id: uuidv4(), user_id: userId, asdfghjkl: 1 }; const { error } = await supabase.from(t).insert([payload]); console.log('Error for ' + t + ':', error?.message || 'Success?'); } } test();
