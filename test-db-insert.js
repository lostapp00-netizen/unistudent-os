import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test() { const { error } = await supabase.from('schedule_items').insert([{ id: 'test', user_id: 'test', subject_id: 'test', day: 0, start_time: '08:00', end_time: '10:00', location: 'Hall 1', type: 'lecture', priority: 'medium', attachments: [] }]); console.log(error); } test();
